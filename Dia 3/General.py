#Propiedades de los strings
# upper () mayusculas
# lower () minusculas
# split () separarlo en partes
# join () unir items usando separador
# find () encontar un sub-string (igual que index pero cuando no encuentra : -1)
# replace () remplazar un substring (remplazar, nueva)


#Listas
# .pop() eliminar
# .append() agregar
# .sort() ordenar alfabeticamente (no devuelve nada)
# .reverse() ordenar al reves (no devuelve nada)

#Diccionarios
# .values para sacar los valores sin las keys
# .keys para sacar las keys sin los valores
# .items para saber todos los elementos del diccionario

#Ejemplo:
#dic = {"c1":["a", "b", "c"], "c2": ["d", "e", "f"] }
#print(dic["c2"][1].upper())

#Tuples: son inmutables
# .count contar el número de apariciones de un valor en un tuple
# .index para buscar los valores
# list() para combertir a lista
# a,b,c para asignar valores del tuple (solo si hay las mismas variables que valores)

#Sets
# .union para unir sets entre sí
# .add añadir valores al set
# .remove eliminar valores al set
# .discard descartar valores al set
# .pop() eliminar elemento aleatorio
# .clear vaciar al set





